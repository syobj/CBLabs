spring:
  mvc:
    throw-exception-if-no-handler-found: true
  resources:
    add-mappings: true
  thymeleaf:
    mode: LEGACYHTML5
    encoding: utf-8
    cache: false
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/test?characterEncoding=utf-8&useSSL=false&autoReconnect=true&autoRecconnectForPools=true
    username: mysqluser
    password: mysqluser123456
server:
  port: 8080
  
  error:
    whitelabel:
      enabled: false
